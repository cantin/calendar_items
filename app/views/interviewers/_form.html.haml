= form_for @interviewer do |f|
  - if @interviewer.errors.any?
    #error_explanation
      %h2= "#{pluralize(@interviewer.errors.count, "error")} prohibited this interviewer from being saved:"
      %ul
        - @interviewer.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
